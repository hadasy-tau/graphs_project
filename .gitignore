# Pipeline artifacts (regenerate with scripts/01..04 or the tests).
# Includes tests/results/graph_runs/, where test_build_graphs.py writes its CSVs -
# a full-corpus run is ~20 MB, which does not belong in git.
data/
results/

# Not ignored: tests/fixtures/*.jsonl is committed on purpose so the
# smoke test runs offline.

__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store
